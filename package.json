{
  "name": "gh-contribution-mate",
  "scripts": {
    "dev": "deno run --allow-read --allow-write --allow-env --allow-run src/main.ts",
    "build": "deno run --allow-run=deno script/compile.ts"
  }
}
